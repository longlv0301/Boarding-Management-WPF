<Window x:Class="QuanLyPhongTro.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        xmlns:local="clr-namespace:QuanLyPhongTro.Views"
        xmlns:viewmodels="clr-namespace:QuanLyPhongTro.ViewModels"
        
        Title="Phần Mềm Quản Lý Phòng Trọ - DEVOLT" Height="768" Width="1024"
        WindowStartupLocation="CenterScreen" Background="#F3F4F6">
    <Window.Resources>
        <DataTemplate DataType="{x:Type viewmodels:RoomViewModel}">
            <local:RoomView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:TenantViewModel}">
            <local:TenantView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:ContractViewModel}">
            <local:ContractView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:InvoiceViewModel}">
            <local:InvoiceView />
        </DataTemplate>
        <DataTemplate DataType="{x:Type viewmodels:MeterReadingViewModel}">
            <local:MeterReadingView />
        </DataTemplate>
    </Window.Resources>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        
        <Border Grid.Column="0" Background="#1F2937">
            <StackPanel Margin="0,30,0,0">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,50">
                    <TextBlock Text="⠿ " FontSize="26" FontWeight="Bold" Foreground="White" VerticalAlignment="Center" />
                    <TextBlock Text="DEVOLT" FontSize="24" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                </StackPanel>

                <Button Content="QUẢN LÝ PHÒNG" Command="{Binding ShowRoomCommand}" Background="LightGray" BorderThickness="0" FontSize="15" Padding="20,15" HorizontalContentAlignment="Left" Cursor="Hand"/>
                <Button Content="KHÁCH THUÊ" Command="{Binding ShowTenantCommand}" Background="LightGray" BorderThickness="0" FontSize="15" Padding="20,15" HorizontalContentAlignment="Left" Cursor="Hand"/>
                <Button Content="HỢP ĐỒNG" Command="{Binding ShowContractCommand}" Background="LightGray" BorderThickness="0" FontSize="15" Padding="20,15" HorizontalContentAlignment="Left" Cursor="Hand"/>
                <Button Content="HÓA ĐƠN" Command="{Binding ShowInvoiceCommand}" Background="LightGray" BorderThickness="0" FontSize="15" Padding="20,15" HorizontalContentAlignment="Left"/>
                <Button Content="CHỐT ĐIỆN NƯỚC" Command="{Binding ShowMeterReadingCommand}" Background="LightGray" BorderThickness="0" FontSize="15" Padding="20,15" HorizontalContentAlignment="Left"/>
            </StackPanel>
        </Border>

        <Grid Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Background="White" BorderBrush="#E5E7EB" BorderThickness="0,0,0,1" Padding="25,0">
    <Grid>
        
        <TextBlock Text="BẢNG ĐIỀU KHIỂN" VerticalAlignment="Center" FontSize="18" FontWeight="Bold" Foreground="#1F2937" HorizontalAlignment="Left"/>

        <Grid HorizontalAlignment="Right" VerticalAlignment="Center">
            
            <ToggleButton x:Name="btnProfile" Cursor="Hand" Background="Transparent" BorderThickness="0">
                <ToggleButton.Template>
                    <ControlTemplate TargetType="ToggleButton">
                        
                        <Border Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="25" Padding="6,6,15,6">
                            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                                
                                <Border Width="32" Height="32" CornerRadius="16" Background="#D1D5DB" Margin="0,0,10,0">
                                    <TextBlock Text="👤" HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="16"/>
                                </Border>
                                
                                <TextBlock VerticalAlignment="Center" Foreground="#374151" FontWeight="SemiBold" FontSize="14">
                                    <Run Text="{Binding CurrentUser.FullName}"/>
                                    <Run Text=" - "/>
                                    <Run Text="{Binding CurrentUser.Role}"/>
                                </TextBlock>
                                
                                <TextBlock Text="▼" FontSize="10" Foreground="#9CA3AF" VerticalAlignment="Center" Margin="10,2,0,0"/>
                                
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </ToggleButton.Template>
            </ToggleButton>

                        <Popup IsOpen="{Binding IsChecked, ElementName=btnProfile}"             StaysOpen="False" AllowsTransparency="True" PlacementTarget="{Binding ElementName=btnProfile}" Placement="Bottom" VerticalOffset="5">
                            <Border Width="{Binding ActualWidth, ElementName=btnProfile}" 
                              Background="White" BorderBrush="#E5E7EB" BorderThickness="1" 
                              CornerRadius="12" Padding="5">

                                <Border.Effect>
                                    <DropShadowEffect Color="Black" Opacity="0.1" BlurRadius="10" ShadowDepth="2"/>
                                </Border.Effect>

                                <Button Command="{Binding LogoutCommand}" Background="Transparent" BorderThickness="0" Cursor="Hand" Padding="10">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                                        <TextBlock Text="🚪" Margin="0,0,10,0"/>
                                        <TextBlock Text="Đăng xuất" Foreground="#EF4444" FontWeight="Bold"/>
                                    </StackPanel>
                                </Button>
                            </Border>
                        </Popup>

                    </Grid>
    </Grid>
</Border>

            <ContentControl Grid.Row="1" Content="{Binding CurrentViewModel}" Margin="20"/>
        </Grid>
    </Grid>
</Window>
